<%= form_for(current_user.active_relationships.find_by(followed_id: @user.id),
    html: { method: :delete }, data: { confirm: t("common.unfollow_confirmation", username: @user.username) },
    remote: true) do |f| %>
  <%= f.submit t("btn.follow"), class: "btn" %>
<% end %>
